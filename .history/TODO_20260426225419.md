# WebRTC Meeting Fix TODO

## Bugs Fixed
1. Race condition on join: `initLocalStream` called before `currentUserId` set → no offers sent
2. Missing tracks on existing peers: empty peer connections never get tracks added later
3. No renegotiation on track replace: remote peers never see screen share / camera changes
4. One-sided `onnegotiationneeded`: only lower ID could initiate renegotiation

## Steps
- [x] Analyze code and identify root causes
- [x] Get user approval for fix plan
- [ ] Edit `apps/web/hooks/useMeetingSocket.ts` — Fix WebRTC logic
- [ ] Edit `apps/web/components/class/ClassMeeting.tsx` — Remove 500ms delay, fix flow
- [ ] Test and verify
